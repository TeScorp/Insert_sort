
insertSort (Tab arr:[i])
  begin
    for  i = 1; i < arr.length; i++
      element = arr[i];
      let j = i - 1;
    while (j >= 0 and arr[j] > element)
      arr[j + 1] = arr[j];
      j--;
  
    arr[j + 1] = element;
    End for





